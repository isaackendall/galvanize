// We are going to complete a function that takes in an array and a number 
// (will refer to an index within the array). The function should return the 
// element located within the array at the given number index.

function returnAnElement(array, index) {
  return array[index];// returns array element at given index
}